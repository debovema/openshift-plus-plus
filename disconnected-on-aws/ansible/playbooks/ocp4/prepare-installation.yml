---
- name: Prepare service VM for OCP
  hosts: bastion
  gather_facts: false
  # vars:
  #   ocp4_org_name: ocp4
  #   local_registry: "{{ public_dns_name }}:8443"
  #   quay_init_username: admin
  #   quay_init_password: quayadmin123
  #   quay_init_email: quayadmin@redhat.io
  roles:
  - role: ocp4-pull-secret
  - role: ocp4-ips
    # name: Define OpenShift Nodes IPs
  - role: ocp4-prepare-installation
  - role: ocp4-installation-files
